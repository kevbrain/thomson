apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app.kubernetes.io/managed-by: EventListener
    app.kubernetes.io/part-of: Triggers
    eventlistener: thomson
  name: el-thomson-dev-route
  namespace: thomson-dev
spec:
  host: el-thomson-dev.apps.ocp-lab.its4u.eu
  port:
    targetPort: http-listener
  to:
    kind: Service
    name: el-thomson-dev-eventlistener
    weight: 100
  wildcardPolicy: None